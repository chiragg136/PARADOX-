{
  "name": "vestocart-monorepo",
  "version": "1.0.0",
  "scripts": {
    "start-backend": "cd backend && node server.js",
    "start-frontend": "cd frontend && npm run dev",
    "dev": "npm-run-all --parallel start-backend start-frontend"
  },
  "devDependencies": {
    "npm-run-all": "^4.1.5"
  }
}
